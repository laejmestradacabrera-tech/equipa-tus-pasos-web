const grid=document.getElementById('catalog-grid'),search=document.getElementById('catalog-search'),status=document.getElementById('catalog-status'),more=document.getElementById('catalog-more'),dialog=document.getElementById('catalog-lightbox');let catalog=[],filtered=[],visible=12;
const normalize=value=>value.normalize('NFD').replace(/[\u0300-\u036f]/g,'').toLowerCase();
const titleFor=item=>item.variants.length?item.variants.map(v=>v.model).join(' · '):item.heading;
function showPage(item){const image=dialog.querySelector('img');image.src=item.image;image.alt=`Página del catálogo con modelos ${titleFor(item)}`;dialog.querySelector('p').textContent=`Modelos ${titleFor(item)} · Consulta precio y disponibilidad`;dialog.showModal()}
function render(){grid.replaceChildren();filtered.slice(0,visible).forEach(item=>{const card=document.createElement('article');card.className='catalog-card';const button=document.createElement('button');button.type='button';button.className='catalog-page-button';button.setAttribute('aria-label',`Ampliar modelos ${titleFor(item)}`);const image=document.createElement('img');image.src=item.image;image.alt=`Modelos ${titleFor(item)} del catálogo Flexi`;image.loading='lazy';button.append(image);button.addEventListener('click',()=>showPage(item));const heading=document.createElement('h3');heading.textContent=item.name||`Modelos ${titleFor(item)}`;const models=document.createElement('p');models.className='catalog-models';models.textContent=item.variants.length?item.variants.map(v=>`${v.model} ${v.color}`).join(' · '):'Información de tecnologías y atributos Flexi';const link=document.createElement('a');link.className='text-link';link.href='index.html#contacto';link.textContent='Consultar precio →';card.append(button,heading,models,link);grid.append(card)});status.textContent=filtered.length===1?'1 página de productos e información encontrada.':`${filtered.length} páginas de productos e información encontradas.`;more.hidden=visible>=filtered.length}
function filterCatalog(){const query=normalize(search.value.trim());filtered=query?catalog.filter(item=>normalize(item.search).includes(query)):catalog;visible=12;render()}
more.addEventListener('click',()=>{visible+=12;render()});search.addEventListener('input',filterCatalog);dialog.querySelector('.lightbox-close').addEventListener('click',()=>dialog.close());dialog.addEventListener('click',event=>{if(event.target===dialog)dialog.close()});
fetch('catalogo.json').then(response=>{if(!response.ok)throw new Error();return response.json()}).then(data=>{catalog=data;filtered=data;render()}).catch(()=>{status.textContent='No fue posible cargar el catálogo. Intenta nuevamente.'});
